Sous linux:
-----------
#create virtualenv:
python3 -m venv .venv

#activate virtualenv:
. .venv/bin/activate

#pip in venv:
[sudo not advised] python -m pip install -r requirements.txt

===========================
Sous windows
---------
#create virtualenv:
python3 -m venv .venv

#activate virtualenv:
.venv\Scripts\activate

#pip in venv:
python -m pip install -r requirements.txt

